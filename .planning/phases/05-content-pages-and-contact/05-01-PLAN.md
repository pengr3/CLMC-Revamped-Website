---
phase: 05-content-pages-and-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/about.ts
  - data/services.ts
  - data/clients.ts
  - data/testimonials.ts
  - app/about/page.tsx
  - app/about/ceo/page.tsx
  - app/services/page.tsx
  - app/clients/page.tsx
  - app/clients/testimonials/page.tsx
  - app/qms/page.tsx
  - components/sections/AboutHeroSection.tsx
  - components/sections/MilestonesSection.tsx
  - components/sections/ValuesSection.tsx
  - components/sections/ServiceDetailSection.tsx
  - components/sections/ClientsGridSection.tsx
  - components/sections/TestimonialsGridSection.tsx
  - components/ui/TestimonialCard.tsx
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - SVC-01
  - CLIENT-01
  - TESTI-01
  - QMS-01

must_haves:
  truths:
    - "Visitor can read the About Us page with company background, mission, timeline milestones, and team values"
    - "Visitor can read the About CEO page with portrait placeholder and bio text in split layout"
    - "Visitor can read the Services page with full descriptions and scope bullet lists for each service"
    - "Visitor can view the Our Clients page with a logo grid showing client names"
    - "Visitor can read the Client Testimony page with testimonial cards in a 2-column grid"
    - "Visitor can view the QMS Policy page with scannable section headings and numbered lists"
  artifacts:
    - path: "data/about.ts"
      provides: "Milestone and values data arrays"
      exports: ["MILESTONES", "VALUES"]
    - path: "data/services.ts"
      provides: "Extended service definitions with scope bullets"
      exports: ["SERVICES_DETAIL"]
    - path: "data/clients.ts"
      provides: "Client logo placeholder data"
      exports: ["CLIENTS"]
    - path: "data/testimonials.ts"
      provides: "Testimonial card data"
      exports: ["testimonials"]
    - path: "app/about/page.tsx"
      provides: "About Us page assembling hero + milestones + values sections"
    - path: "app/about/ceo/page.tsx"
      provides: "About CEO page with split layout"
    - path: "app/services/page.tsx"
      provides: "Services page with detail sections"
    - path: "app/clients/page.tsx"
      provides: "Our Clients page with logo grid"
    - path: "app/clients/testimonials/page.tsx"
      provides: "Client Testimonials page with card grid"
    - path: "app/qms/page.tsx"
      provides: "QMS Policy page with structured content"
  key_links:
    - from: "components/sections/MilestonesSection.tsx"
      to: "data/about.ts"
      via: "import MILESTONES"
      pattern: "import.*MILESTONES.*from.*data/about"
    - from: "components/sections/ServiceDetailSection.tsx"
      to: "data/services.ts"
      via: "import SERVICES_DETAIL"
      pattern: "import.*SERVICES_DETAIL.*from.*data/services"
    - from: "components/sections/ClientsGridSection.tsx"
      to: "data/clients.ts"
      via: "import CLIENTS"
      pattern: "import.*CLIENTS.*from.*data/clients"
    - from: "components/sections/TestimonialsGridSection.tsx"
      to: "data/testimonials.ts"
      via: "import testimonials"
      pattern: "import.*testimonials.*from.*data/testimonials"
---

<objective>
Build all six content pages for Phase 5: About Us, About CEO, Services, Our Clients, Client Testimonials, and QMS Policy. Each page uses placeholder content, follows the established Server Component + client animation wrapper pattern, and reuses useFadeUp/useStagger from Phase 2.

Purpose: Complete the information architecture so visitors can browse all CLMC content pages with consistent design and animation patterns.
Output: 6 working page routes, 4 data files, 7 section components, 1 UI atom.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-pages-and-contact/05-CONTEXT.md
@.planning/phases/05-content-pages-and-contact/05-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From components/animation/useFadeUp.ts and useStagger.ts:
```typescript
export function useFadeUp(ref: React.RefObject<HTMLElement | null>): void
export function useStagger(ref: React.RefObject<HTMLElement | null>): void
```

From components/ui/ServiceCard.tsx:
```typescript
interface ServiceCardProps {
  icon: LucideIcon
  title: string
  description: string
  href?: string
  className?: string
}
export { ServiceCard }
```

From data/projects.ts (data file pattern):
```typescript
export interface Project { id: string; title: string; /* ... */ }
export const BLUR_DATA_URL = 'data:image/png;base64,...'
```

From app/globals.css (design tokens):
```
--color-surface-primary: #0D0D0D
--color-surface-secondary: #141414
--color-text-primary: #F5F5F5
--color-text-secondary: rgba(255, 255, 255, 0.60)
--color-text-muted: rgba(255, 255, 255, 0.35)
--spacing-lg: 24px  --spacing-xl: 32px  --spacing-2xl: 48px
--spacing-3xl: 64px  --spacing-4xl: 96px  --spacing-5xl: 128px
```

Tailwind class mapping: bg-surface-primary, text-text-primary, py-4xl, px-md, md:px-2xl, font-display, font-body, etc.

Animation data attributes: data-fade-up, data-stagger, data-stagger-child
- data-stagger-child goes on wrapper <div>, NOT on component directly
- Every section using data-fade-up MUST be 'use client' with useFadeUp hook called
- Sections without animation can be Server Components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data files and About/CEO/Services pages</name>
  <files>
    data/about.ts,
    data/services.ts,
    components/sections/AboutHeroSection.tsx,
    components/sections/MilestonesSection.tsx,
    components/sections/ValuesSection.tsx,
    components/sections/ServiceDetailSection.tsx,
    app/about/page.tsx,
    app/about/ceo/page.tsx,
    app/services/page.tsx
  </files>
  <read_first>
    components/sections/ServicesSection.tsx,
    components/ui/ServiceCard.tsx,
    components/sections/HeroSection.tsx,
    data/projects.ts,
    app/about/page.tsx,
    app/services/page.tsx,
    app/globals.css
  </read_first>
  <action>
    **Step 1: Create data/about.ts**
    Define two typed arrays:
    - `Milestone` interface: `{ year: string; event: string }`
    - `MILESTONES` array with 5-6 entries spanning CLMC's history (placeholder years/events, e.g., "1995" / "C. Lacsamana Management and Construction Corporation founded in Metro Manila", "2005" / "Expanded into commercial fit-out services", etc.)
    - `Value` interface: `{ title: string; description: string }`
    - `VALUES` array with 4 entries (e.g., "Excellence", "Integrity", "Innovation", "Client-First")

    **Step 2: Create data/services.ts**
    Define `ServiceDetail` interface: `{ id: string; icon: string; title: string; description: string; scope: string[] }`
    - `icon` field stores the Lucide icon name as string (e.g., "Building2") — import mapping happens in the component
    - `SERVICES_DETAIL` array with the same 5 services from ServicesSection.tsx but with expanded description paragraphs (3-4 sentences each) and 4-5 scope bullet points per service
    - Export the Lucide icon mapping: `ICON_MAP: Record<string, LucideIcon>` mapping icon name strings to Lucide components (Building2, Home, Wrench, Hammer, KeyRound)

    **Step 3: Create components/sections/AboutHeroSection.tsx**
    - Server Component (no animation — hero renders immediately, consistent with Phase 4 detail page pattern)
    - Full-width banner section with bg-surface-secondary
    - Large heading: "About CLMC" in font-display text-4xl md:text-5xl lg:text-6xl font-bold text-text-primary
    - Subtitle paragraph: placeholder text about CLMC being a Philippine construction and management consultancy (2-3 sentences), font-body text-lg text-text-secondary
    - Padding: pt-5xl pb-4xl px-md md:px-2xl, max-w-7xl mx-auto

    **Step 4: Create components/sections/MilestonesSection.tsx**
    - 'use client' component — needs useFadeUp + useStagger
    - Import MILESTONES from @/data/about
    - useRef<HTMLElement>(null), call useFadeUp(sectionRef) and useStagger(sectionRef)
    - Section heading "Our Journey" with data-fade-up
    - Timeline layout: vertical left border (border-l-2 border-surface-border), each milestone in a div with data-stagger-child on the wrapper div
    - Each milestone: year in font-display text-sm font-bold text-text-muted, event in font-body text-text-secondary
    - Container: py-4xl px-md md:px-2xl bg-surface-primary, max-w-7xl mx-auto

    **Step 5: Create components/sections/ValuesSection.tsx**
    - 'use client' component — needs useFadeUp + useStagger
    - Import VALUES from @/data/about
    - Section heading "Our Values" with data-fade-up
    - Grid: grid-cols-1 md:grid-cols-2 gap-lg with data-stagger
    - Each value card in a wrapper div with data-stagger-child: border border-surface-border rounded-md p-lg bg-surface-secondary/50
    - Value title: font-display text-lg font-bold text-text-primary mb-sm
    - Value description: font-body text-sm text-text-secondary leading-relaxed

    **Step 6: Create components/sections/ServiceDetailSection.tsx**
    - 'use client' component — needs useFadeUp + useStagger
    - Import SERVICES_DETAIL and ICON_MAP from @/data/services
    - For each service: large section with icon (h-10 w-10), title (font-display text-2xl font-bold), full description paragraph, and scope bullets in a ul with list-disc
    - Use data-stagger on the services container, data-stagger-child on each service wrapper div
    - Section heading "What We Do" with data-fade-up
    - Each service block separated by border-b border-surface-border (except last), py-xl spacing

    **Step 7: Rewrite app/about/page.tsx**
    - Server Component (no 'use client')
    - Import and compose: AboutHeroSection, MilestonesSection, ValuesSection
    - Wrap in <main> tag
    - Add a mission statement section between hero and milestones: a simple Server Component inline div with max-w-3xl mx-auto text-center, heading "Our Mission", and 2-3 sentence placeholder mission statement in font-body text-lg text-text-secondary

    **Step 8: Create app/about/ceo/page.tsx**
    - Server Component — no animation (single-viewport leadership profile renders immediately, per Phase 4 detail page decision and RESEARCH anti-pattern guidance)
    - Split layout: grid grid-cols-1 lg:grid-cols-2 gap-3xl items-start
    - Left: Portrait placeholder — relative aspect-[3/4] w-full max-w-md mx-auto lg:mx-0 rounded-md overflow-hidden bg-surface-secondary, with centered "Portrait Photo" text in text-text-muted
    - Right: CEO name placeholder "[CEO Name]" in font-display text-4xl md:text-5xl font-bold, role "President & CEO" in text-text-secondary, then 2-3 placeholder bio paragraphs in font-body text-text-secondary text-lg leading-relaxed
    - Container: pt-5xl pb-4xl px-md md:px-2xl bg-surface-primary, max-w-7xl mx-auto

    **Step 9: Rewrite app/services/page.tsx**
    - Server Component
    - Hero section at top (same pattern as AboutHeroSection): heading "Our Services", subtitle placeholder describing CLMC service range
    - Import ServiceDetailSection
    - Compose: hero div + ServiceDetailSection in <main>
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/CLMC-Revamped-Website && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - data/about.ts exports MILESTONES array with at least 5 items and VALUES array with 4 items
    - data/services.ts exports SERVICES_DETAIL array with 5 items each having scope array, and ICON_MAP
    - app/about/page.tsx imports AboutHeroSection, MilestonesSection, ValuesSection
    - app/about/ceo/page.tsx has grid grid-cols-1 lg:grid-cols-2 layout with portrait placeholder
    - app/services/page.tsx imports ServiceDetailSection
    - All 3 routes build without error: /about, /about/ceo, /services
    - MilestonesSection.tsx has 'use client' directive and calls useFadeUp and useStagger
    - ValuesSection.tsx has 'use client' directive and calls useFadeUp and useStagger
    - ServiceDetailSection.tsx has 'use client' directive and calls useFadeUp and useStagger
    - AboutHeroSection.tsx does NOT have 'use client' (Server Component)
    - data-stagger-child is on wrapper divs, not on imported components
  </acceptance_criteria>
  <done>About Us page shows hero + mission + milestones + values, About CEO page shows split portrait/bio layout, Services page shows expanded service details with scope bullets. All three routes render without build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Clients, Testimonials, and QMS pages</name>
  <files>
    data/clients.ts,
    data/testimonials.ts,
    components/ui/TestimonialCard.tsx,
    components/sections/ClientsGridSection.tsx,
    components/sections/TestimonialsGridSection.tsx,
    app/clients/page.tsx,
    app/clients/testimonials/page.tsx,
    app/qms/page.tsx
  </files>
  <read_first>
    components/sections/ClientLogosSection.tsx,
    data/projects.ts,
    app/clients/page.tsx,
    app/globals.css
  </read_first>
  <action>
    **Step 1: Create data/clients.ts**
    Define `Client` interface: `{ id: string; name: string; industry?: string }`
    Export `CLIENTS` array with 12 placeholder entries (e.g., "Ayala Corporation", "SM Prime Holdings", "Department of Public Works and Highways", etc. — use recognizable Philippine companies/agencies as placeholders).

    **Step 2: Create data/testimonials.ts**
    Define `Testimonial` interface: `{ id: string; quote: string; clientName: string; company: string; role: string }`
    Export `testimonials` array with 6 placeholder entries. Each quote should be 2-3 sentences of realistic construction-industry testimonial text (e.g., "CLMC delivered our office renovation ahead of schedule and within budget. Their project management team maintained clear communication throughout the entire process.").

    **Step 3: Create components/ui/TestimonialCard.tsx**
    - Server Component (pure display, no animation hooks needed — wrapper handles stagger)
    - Props: `{ quote: string; clientName: string; company: string; role: string; className?: string }`
    - Card layout: border border-surface-border rounded-md p-lg bg-surface-secondary/50
    - Opening quotation mark: font-display text-4xl text-text-muted leading-none mb-sm (use the character)
    - Quote text: font-body text-text-secondary text-sm leading-relaxed italic
    - Divider: border-t border-surface-border mt-lg pt-md
    - Client name: font-display text-sm font-bold text-text-primary
    - Role + company: font-body text-xs text-text-muted

    **Step 4: Create components/sections/ClientsGridSection.tsx**
    - 'use client' component — needs useFadeUp + useStagger
    - Import CLIENTS from @/data/clients
    - Section heading "Our Clients" with data-fade-up
    - Grid: grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-lg with data-stagger
    - Each client in a wrapper div with data-stagger-child: flex flex-col items-center gap-sm p-lg border border-surface-border rounded-md bg-surface-secondary/50
    - Logo placeholder: h-12 w-32 flex items-center justify-center, inner div h-8 w-24 rounded bg-surface-tertiary
    - Client name below: font-body text-sm text-text-secondary text-center
    - Industry if present: font-body text-xs text-text-muted

    **Step 5: Create components/sections/TestimonialsGridSection.tsx**
    - 'use client' component — needs useFadeUp + useStagger
    - Import testimonials from @/data/testimonials
    - Import TestimonialCard from @/components/ui/TestimonialCard
    - Section heading "What Our Clients Say" with data-fade-up
    - Grid: grid-cols-1 md:grid-cols-2 gap-lg with data-stagger
    - Each testimonial in a wrapper div with data-stagger-child, rendering TestimonialCard inside

    **Step 6: Rewrite app/clients/page.tsx**
    - Server Component
    - Hero section at top: heading "Our Clients", subtitle about CLMC's trusted partnerships
    - Import and render ClientsGridSection
    - Add a bottom CTA text linking to /clients/testimonials: "Read what our clients say about working with us" using a Link styled with text-text-secondary hover:text-interactive-hover underline

    **Step 7: Create app/clients/testimonials/page.tsx**
    - Server Component (new nested route)
    - Hero section: heading "Client Testimonials", subtitle about client satisfaction
    - Import and render TestimonialsGridSection
    - Breadcrumb-style link back to /clients at top: "Our Clients / Testimonials"

    **Step 8: Create app/qms/page.tsx**
    - Server Component — NO animation hooks (scannable document page renders immediately, like CEO page)
    - Container: pt-5xl pb-4xl px-md md:px-2xl bg-surface-primary, max-w-3xl mx-auto (narrower for readability)
    - Heading: "Quality Management System Policy" in font-display text-3xl md:text-4xl font-bold text-text-primary mb-3xl
    - Content as inline JSX prose (not a data file — per RESEARCH recommendation for static policy documents):
      - Section 1: "Quality Policy Statement" — 2 paragraph placeholder
      - Section 2: "Scope" — 1 paragraph + numbered list (4 items)
      - Section 3: "Quality Objectives" — numbered list (5 items)
      - Section 4: "Management Commitment" — 2 paragraphs
      - Section 5: "Continual Improvement" — 1 paragraph
    - Each section heading: font-display text-xl font-bold text-text-primary mt-2xl mb-md
    - Paragraphs: font-body text-text-secondary leading-relaxed mb-md
    - Numbered lists: list-decimal pl-lg font-body text-text-secondary leading-relaxed, each li with mb-sm
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/CLMC-Revamped-Website && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - data/clients.ts exports CLIENTS array with 12 items
    - data/testimonials.ts exports testimonials array with 6 items each having quote, clientName, company, role
    - components/ui/TestimonialCard.tsx is a Server Component (no 'use client') with quote, clientName, company, role props
    - ClientsGridSection.tsx has 'use client' and calls useFadeUp and useStagger
    - TestimonialsGridSection.tsx has 'use client' and calls useFadeUp and useStagger
    - app/clients/page.tsx imports ClientsGridSection and has link to /clients/testimonials
    - app/clients/testimonials/page.tsx imports TestimonialsGridSection
    - app/qms/page.tsx contains "Quality Management System Policy" heading and at least 5 section headings
    - app/qms/page.tsx does NOT have 'use client' directive
    - All 3 routes build without error: /clients, /clients/testimonials, /qms
    - data-stagger-child is on wrapper divs in ClientsGridSection and TestimonialsGridSection
  </acceptance_criteria>
  <done>Clients page shows logo grid with 12 placeholders, Testimonials page shows 6 testimonial cards in 2-column grid, QMS page shows scannable policy document. All three routes render without build errors.</done>
</task>

</tasks>

<verification>
All 6 content page routes resolve without build errors:
- `npm run build` completes successfully
- Routes /about, /about/ceo, /services, /clients, /clients/testimonials, /qms all present in build output
- No TypeScript errors, no missing imports
- `npm run lint` passes
</verification>

<success_criteria>
1. All 6 page routes render with placeholder content and consistent dark theme styling
2. Animated sections (Milestones, Values, ServiceDetail, ClientsGrid, TestimonialsGrid) use useFadeUp + useStagger with data attributes
3. Non-animated pages (CEO, QMS) render content immediately as Server Components
4. Data files follow the typed array pattern established by data/projects.ts
5. Build passes cleanly with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-pages-and-contact/05-01-SUMMARY.md`
</output>
