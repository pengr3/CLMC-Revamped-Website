---
phase: 05-content-pages-and-contact
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/contact/page.tsx
  - components/sections/ContactFormSection.tsx
  - components/layout/Footer.tsx
  - .env.local
autonomous: true
requirements:
  - CTCT-01
  - CTCT-02
  - CTCT-03

user_setup:
  - service: formspree
    why: "Contact form email delivery"
    env_vars:
      - name: NEXT_PUBLIC_FORMSPREE_ID
        source: "Formspree Dashboard -> Create Form -> Copy Form ID (e.g., 'xrgvnpkz')"
    dashboard_config:
      - task: "Create a Formspree account and form"
        location: "https://formspree.io -> Sign Up -> New Form -> Copy form ID"

must_haves:
  truths:
    - "Visitor can access /contact from the site navigation"
    - "Visitor can fill out and submit an inquiry form with name, email, phone, subject, and message"
    - "Visitor sees a success message after form submission"
    - "Visitor sees CLMC phone and email in the Footer on all pages"
    - "Visitor sees contact info (phone, email, office address) on the Contact page"
  artifacts:
    - path: "components/sections/ContactFormSection.tsx"
      provides: "Interactive contact form with Formspree integration"
      contains: "'use client'"
    - path: "app/contact/page.tsx"
      provides: "Contact page with form + info card layout"
    - path: ".env.local"
      provides: "Formspree placeholder environment variable"
      contains: "NEXT_PUBLIC_FORMSPREE_ID"
  key_links:
    - from: "components/sections/ContactFormSection.tsx"
      to: "@formspree/react"
      via: "useForm hook"
      pattern: "useForm.*NEXT_PUBLIC_FORMSPREE_ID"
    - from: "app/contact/page.tsx"
      to: "components/sections/ContactFormSection.tsx"
      via: "import ContactFormSection"
      pattern: "import.*ContactFormSection"
    - from: "components/layout/Footer.tsx"
      to: "tel: and mailto: links"
      via: "anchor elements"
      pattern: "href=\"tel:|href=\"mailto:"
---

<objective>
Build the Contact page with a working Formspree-powered inquiry form, contact info card, and verify Footer contact information is present for CTCT-02 compliance.

Purpose: Give visitors a way to reach CLMC through a premium contact form experience, completing the site's conversion funnel.
Output: Working /contact page with form + info card, Formspree integration, verified Footer contact info.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-pages-and-contact/05-CONTEXT.md
@.planning/phases/05-content-pages-and-contact/05-RESEARCH.md
@.planning/phases/05-content-pages-and-contact/05-01-SUMMARY.md

<interfaces>
<!-- Existing patterns the executor needs -->

From components/ui/Button.tsx:
```typescript
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost'
  size?: 'sm' | 'md' | 'lg'
}
export { Button, buttonVariants }
```

From components/layout/Footer.tsx (contact info already present):
```tsx
// Column 3: Contact info
<a href="tel:+63XXXXXXXXXX">+63 (XX) XXX-XXXX</a>
<a href="mailto:info@clmc.ph">info@clmc.ph</a>
```

From app/globals.css (form-relevant tokens):
```
--color-surface-secondary: #141414
--color-surface-border: rgba(255, 255, 255, 0.08)
--color-text-primary: #F5F5F5
--color-text-secondary: rgba(255, 255, 255, 0.60)
--color-destructive: #EF4444
--color-interactive-hover: rgba(255, 255, 255, 0.70)
```

@formspree/react API (v3.0.0):
```typescript
import { useForm, ValidationError } from '@formspree/react'
const [state, handleSubmit] = useForm('form-id')
// state.succeeded: boolean — form submitted successfully
// state.submitting: boolean — submission in progress
// state.errors: FieldErrors — validation/submission errors
// ValidationError: component that displays field-level errors
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Formspree and build Contact page with form and info card</name>
  <files>
    .env.local,
    components/sections/ContactFormSection.tsx,
    app/contact/page.tsx
  </files>
  <read_first>
    components/ui/Button.tsx,
    components/layout/Footer.tsx,
    app/contact/page.tsx,
    app/globals.css,
    package.json
  </read_first>
  <action>
    **Step 1: Install @formspree/react**
    Run: `npm install @formspree/react`

    **Step 2: Create .env.local**
    Add single line: `NEXT_PUBLIC_FORMSPREE_ID=placeholder`
    This allows the form to render without error. Submissions will 404 gracefully until user configures a real Formspree form ID.

    **Step 3: Create components/sections/ContactFormSection.tsx**
    - 'use client' component
    - Import { useForm, ValidationError } from '@formspree/react'
    - Import { Button } from '@/components/ui/Button'
    - SUBJECT_OPTIONS array: ['General Inquiry', 'Project Consultation', 'Request for Inspection']
    - useForm(process.env.NEXT_PUBLIC_FORMSPREE_ID ?? 'placeholder')

    Success state (when state.succeeded):
    - Centered div with py-2xl
    - Heading: "Message Received" in font-display text-2xl font-bold text-text-primary
    - Subtext: "Thank you for reaching out. We will be in touch shortly." in font-body text-text-secondary mt-md

    Form (default state):
    - <form onSubmit={handleSubmit} className="flex flex-col gap-lg" noValidate>
    - Fields (each in a flex flex-col gap-xs wrapper):
      1. Name: <label htmlFor="name"> + <input id="name" name="name" type="text" required> + <ValidationError field="name" prefix="Name" errors={state.errors}>
      2. Email: <label htmlFor="email"> + <input id="email" name="email" type="email" required> + <ValidationError field="email" prefix="Email" errors={state.errors}>
      3. Phone: <label htmlFor="phone"> + <input id="phone" name="phone" type="tel"> (not required) + <ValidationError field="phone" prefix="Phone" errors={state.errors}>
      4. Subject: <label htmlFor="subject"> + <select id="subject" name="subject" className="appearance-none"> with SUBJECT_OPTIONS mapped to <option>
      5. Message: <label htmlFor="message"> + <textarea id="message" name="message" rows={6} required> + <ValidationError field="message" prefix="Message" errors={state.errors}>
    - General error display: <ValidationError errors={state.errors} className="text-destructive text-sm">
    - Submit button: <Button type="submit" variant="primary" size="md" disabled={state.submitting}>{state.submitting ? 'Sending...' : 'Send Message'}</Button>

    Input styling (shared across all inputs, select, textarea):
    `bg-surface-secondary border border-surface-border rounded-md px-md py-sm font-body text-text-primary focus:outline-none focus-visible:border-white/40`
    - Textarea adds: resize-none
    - Select adds: appearance-none
    - Label styling: font-body text-sm text-text-secondary
    - ValidationError className: text-destructive text-sm

    Add comment at top of file:
    ```
    // TODO: Replace NEXT_PUBLIC_FORMSPREE_ID in .env.local with your real Formspree form ID
    // Create a form at https://formspree.io and copy the form ID
    ```

    **Step 4: Rewrite app/contact/page.tsx**
    - Server Component (no 'use client')
    - Import ContactFormSection
    - Layout: two-column on desktop (grid grid-cols-1 lg:grid-cols-2 gap-3xl), stacked on mobile
    - Container: pt-5xl pb-4xl px-md md:px-2xl bg-surface-primary, max-w-7xl mx-auto

    Left column — Form:
    - Heading: "Get in Touch" in font-display text-3xl md:text-4xl font-bold text-text-primary mb-md
    - Subtitle: "Have a project in mind? We'd love to hear from you." in font-body text-text-secondary mb-xl
    - Render <ContactFormSection />

    Right column — Contact Info Card:
    - Sticky card: sticky top-[120px] (accounts for navbar)
    - Card: border border-surface-border rounded-md p-xl bg-surface-secondary/50
    - Heading: "Contact Information" in font-display text-lg font-bold text-text-primary mb-lg
    - Three info blocks (each with mb-lg):
      1. Phone: label "Phone" in font-body text-xs text-text-muted uppercase tracking-wider mb-xs, then <a href="tel:+63XXXXXXXXXX" className="font-body text-text-secondary hover:text-interactive-hover transition-colors">+63 (XX) XXX-XXXX</a>
      2. Email: label "Email", then <a href="mailto:info@clmc.ph" className="...">info@clmc.ph</a>
      3. Office: label "Office", then <p className="font-body text-text-secondary">[Placeholder Address], Metro Manila, Philippines</p>
    - Map placeholder at bottom: div with h-48 rounded-md bg-surface-tertiary flex items-center justify-center, text "Map" in text-text-muted text-sm
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/CLMC-Revamped-Website && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - @formspree/react is in package.json dependencies
    - .env.local contains NEXT_PUBLIC_FORMSPREE_ID=placeholder
    - ContactFormSection.tsx has 'use client' directive
    - ContactFormSection.tsx imports useForm and ValidationError from @formspree/react
    - ContactFormSection.tsx has all 5 form fields: name, email, phone, subject (select), message (textarea)
    - ContactFormSection.tsx handles state.succeeded with success message
    - ContactFormSection.tsx handles state.submitting with disabled button and "Sending..." text
    - app/contact/page.tsx is a Server Component (no 'use client')
    - app/contact/page.tsx has grid grid-cols-1 lg:grid-cols-2 layout
    - app/contact/page.tsx has contact info card with phone (+63), email (info@clmc.ph), and office address
    - npm run build completes without errors
    - Footer.tsx already has tel: and mailto: links (CTCT-02 verified — no changes needed to Footer)
  </acceptance_criteria>
  <done>Contact page shows two-column layout with working form (fields: name, email, phone, subject dropdown, message) on left and contact info card on right. Form handles submission states (submitting, succeeded, errors). Phone and email visible in both Footer and contact info card. Build passes.</done>
</task>

</tasks>

<verification>
Contact page complete:
- `npm run build` passes
- /contact route renders with form + info card
- ContactFormSection handles all three states: default form, submitting, succeeded
- Footer contact info present (phone + email) — CTCT-02
- /contact accessible from existing Navbar Contact link — CTCT-03
- All Phase 5 routes build: /about, /about/ceo, /services, /clients, /clients/testimonials, /qms, /contact
</verification>

<success_criteria>
1. Contact form renders with all 5 fields per D-10
2. Form submission uses @formspree/react useForm hook per D-11
3. Two-column layout (form left, info right) per D-12, stacked on mobile
4. Contact info card shows phone, email, and office address per D-13
5. Footer already has phone/email (CTCT-02 verified)
6. /contact accessible from Navbar (CTCT-03 verified)
7. Build passes with no TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-pages-and-contact/05-02-SUMMARY.md`
</output>
